name: tama-id-au

default-extensions:
  - OverloadedStrings
  - ExtendedDefaultRules

ghc-options:
  - -Wall

dependencies:
  - base
  - containers
  - lucid
  - clay
  - text
  - bytestring
  - directory
  - filepath
  - base64
  - uri-encode

executable:
  main: Main.hs
  source-dirs: src